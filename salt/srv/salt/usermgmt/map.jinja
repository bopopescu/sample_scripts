{% set sshcfg = salt['grains.filter_by']({
    'RedHat': {
        'service': 'sshd',
        'config': '/etc/ssh/sshd_config',
    },
    'Solaris': {
        'service': 'ssh',
        'config': '/etc/ssh/sshd_config',
    },
    'Debian': {
        'service': 'ssh',
        'config': '/etc/ssh/sshd_config',
    },
}, grain='os_family',merge=salt['pillar.get']('sshcfg:lookup')) %}
